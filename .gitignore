# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri
backends/target/*
# 注意：backends/binaries/ 中的 adb sidecar 已纳入 git（CI 构建依赖）
# 仅排除运行时下载/生成的临时文件
backends/binaries/.tmp/
backends/binaries/*.bak

# Mock Server
mock_server/

# history
.history

# claude code rule
CLAUDE.md

# Tauri updater signing keys (NEVER commit private keys)
*.key
*.key.pub
.tauri/

# Release artifacts
dist-release/